# next-content-overlay demo

This demo proves the core flow in under 5 minutes:

1. `init`
2. `scan`
3. `edit`
4. `publish`

## Run

```bash
npm install
npm run overlay:flow
npm run dev
```

Open `http://localhost:3000` and confirm the headline changed to:

`Shipped in minutes with content-overlay`
